<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.*" />
    <PackageReference Include="xunit" Version="2.*" />
    <PackageReference Include="xunit.runner.visualstudio" Version="4.*" />
    <PackageReference Include="Avalonia.Headless" Version="12.1.*" />
  </ItemGroup>

  <!-- The launch chain is the part of this app that broke in ways manual testing could not reach:
       a restart racing its own relaunch, a chain outliving the tile it belonged to. Terminal.Avalonia
       exposes PtyFactory so it can be driven without spawning a shell — that is what these use. -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\mTiles\mTiles.csproj" />
    <ProjectReference Include="..\..\src\mTiles.AgentSessions\mTiles.AgentSessions.csproj" />
  </ItemGroup>

</Project>
