<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:mTiles.ViewModels"
             xmlns:views="using:mTiles.Views"
             xmlns:mi="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
             x:Class="mTiles.App"
             RequestedThemeVariant="Dark">
    <Application.Styles>
        <FluentTheme />
        <StyleInclude Source="avares://AvaloniaEdit/Themes/Fluent/AvaloniaEdit.xaml" />
        <mi:MaterialIconStyles />
        <StyleInclude Source="avares://mTiles/Styles/Controls.axaml" />
        <!-- The controls that are not this application's: see src/mTiles.Controls. They name role tokens
             and define none, so this line has to come after AppTheme has something to answer with — which
             it does, resources being resolved when they are read rather than when they are included. -->
        <StyleInclude Source="avares://mTiles.Controls/Themes/Picker.axaml" />
    </Application.Styles>

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceInclude Source="avares://mTiles/Styles/AppTheme.axaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>

    <Application.DataTemplates>
        <DataTemplate DataType="vm:TerminalTileViewModel">
            <views:TerminalTileView />
        </DataTemplate>
        <DataTemplate DataType="vm:NoteTileViewModel">
            <views:NoteTileView />
        </DataTemplate>
        <DataTemplate DataType="vm:SettingsViewModel">
            <views:SettingsView />
        </DataTemplate>
    </Application.DataTemplates>
</Application>
